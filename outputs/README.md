# Outputs

This directory is for generated artifacts and local experiment outputs.

Recommended local structure:

```text
outputs/
├── figures/
├── metrics/
├── predictions/
└── splits/
```

Only small documentation files should be committed. Generated CSVs, figures, checkpoints and predictions should usually stay local unless they are needed for a report.
